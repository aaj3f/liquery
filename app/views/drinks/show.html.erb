<section class="pricing pt-5">
  <div class="row pb-5 mt-5">
    <div class="col-xl-3 col-l-3 offset-xl-2 offset-l-2 mb-4 show-container" style="display:flex;justify-content:center;align-items:center;">
        <div class="card border-0 shadow drink-list">
          <img class="show-img" src="<%= @drink.image %>" class="card-img-top" alt="<%= @drink.name %>">
        </div>
    </div>

    <div class="col-xl-5 col-l-5" style="display:flex;justify-content:center;align-items:flex-end;">
        <div class="card border-0 shadow drink-list">

          <div class="card-body text-center py-4 px-5">
            <h2 class="mb-3"><%= @drink.name %></h2>
            <div class="card-text text-black-50">
              <p><strong>Ingredients</strong></p>
                  <% @drink.measures.each do |m| %>
                  <p><%= m.size %> <%= m.measurement_type %> of <%= m.ingredient.name %>
                  <% if m.ingredient.flavor_profile %><br><em><%= "contributes to #{m.ingredient.flavor_profile.name.downcase} palate" %></em><% end %></p>
                  <% end %>
              <p><strong>Preparation</strong></p>
              <p><%= @drink.preparation %></p>
          </div>
          <%= like_or_liked %>

        </div>
    </div>
  </div>
</section>
